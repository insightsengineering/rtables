# Length of a Cell value

Length of a Cell value

## Usage

``` r
# S4 method for class 'CellValue'
length(x)
```

## Arguments

- x:

  (`CellValue`)  
  a `CellValue` object.

## Value

Always returns `1L`.
