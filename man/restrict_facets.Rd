% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_split_fun.R
\name{restrict_facets}
\alias{restrict_facets}
\title{Postprocessing split function behavior to generally restrict facets}
\usage{
restrict_facets(
  facets,
  op = c("keep", "exclude"),
  reorder = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{facets}{\code{(character)}\cr Vector of facet names}

\item{op}{\verb{("keep", or "exclude")}\cr Whether \code{facets} names facets
to be (exclusively) kept (the default) or removed.}

\item{reorder}{\code{(flag)}\cr For \code{op == "keep"}, should the resulting
facets be reordered to the order they appear in
\code{facets}. Defaults to \code{TRUE}.}

\item{quiet}{\code{(logical(1))}\cr Whether warnings should be given or
not (the default) when facets named in \code{facets} are not found
in the split result.}
}
\value{
a function suitable for use within the \code{post} argument of
\code{\link[=make_split_fun]{make_split_fun()}}.
}
\description{
Postprocessing split function behavior to generally restrict facets
}
\details{
This is a function factory which creates a post-process
behavioral building block for use in \code{\link[=make_split_fun]{make_split_fun()}}.

This factory provides the equivalent of both \code{keep_split_levels}
and \code{remove_split_levels} in a form suitable for use in
\code{\link[=make_split_fun]{make_split_fun()}}.

When \code{op} is \code{"keep"} (the default), resulting facets are
restricted to only those named in \code{facets} when the generated
function is applied to a split result; in the case of \code{"exclude"},
facets named in \code{facets} are removed so that only those not named
remain.

The generated function will throw a warning if any of \code{facets} are
not found in the split result it receives during splitting, unless
it was created with \code{quietly = FALSE}.
}
\seealso{
\code{\link[=make_split_fun]{make_split_fun()}}

Other make_custom_split: 
\code{\link{add_combo_facet}()},
\code{\link{drop_facet_levels}()},
\code{\link{make_split_fun}()},
\code{\link{make_split_result}()},
\code{\link{trim_levels_in_facets}()}
}
\concept{make_custom_split}
