% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rtable.R
\name{rtable}
\alias{rtable}
\title{Create a Reporting Table}
\usage{
rtable(header, ..., format = "xx")
}
\arguments{
\item{header}{either a vector with column names or an object returned by
\code{\link{rheader}} if special formating and multi-line headers are
needed}

\item{...}{each element is an \code{\link{rrow}} object}

\item{format}{a valid format string or a format function for
\code{\link{rcell}}s. To get a list of all valid format strings use
\code{\link{list_rcell_format_labels}}. If format is \code{NULL} then the elements
of a cell get pasted separated by a comma.}
}
\value{
an \code{rtable} object
}
\description{
Reporting tables allow multiple values per cell, cell formatting and merging
cells. Currently an \code{rtable} can be converted to html and ascii.
}
\details{
Rtable objects can be currently exported to text with
\code{\link{toString}} and to html with \code{\link{as_html}}. In
future we would plan to add the \code{as_latex} and \code{as_gridGrob}
outputting function.

Note that the formats propagate to the \code{\link{rrow}} and 
\code{\link{rcell}} if these do not specify their own format.
}
\examples{

tbl <- rtable(
  header = c("Treatement\\nN=100", "Comparison\\nN=300"),
  format = "xx (xx.xx\%)",
  rrow("A", c(104, .2), c(100, .4)),
  rrow("B", c(23, .4), c(43, .5)),
  rrow(),
  rrow("this is a very long section header"),
  rrow("estimate", rcell(55.23, "xx.xx", colspan = 2)),
  rrow("95\% CI", indent = 1, rcell(c(44.8, 67.4), format = "(xx.x, xx.x)", colspan = 2))
)

tbl

row.names(tbl)
names(tbl)


tbl[1,2]

tbl[3,2]
tbl[5,1]
tbl[5,2]


dim(tbl)
nrow(tbl)
ncol(tbl)

tbl[[1]][[1]]

as_html(tbl)

\dontrun{
Viewer(tbl)
}

# colspans

tbl2 <- rtable(
  c("A", "B", "C", "D", "E"),
  format = "xx",
  rrow("r1", 1, 2, 3, 4, 5),
  rrow("r2", rcell("sp2", colspan = 2), "sp1", rcell("sp2-2", colspan = 2))
)

tbl2

\dontrun{
Viewer(tbl2)
}

tbl2[1,3]
tbl2[2,1]
tbl2[2,2]
tbl2[2,3]
tbl2[2,4]
tbl2[2,5]

# Multi-header table

iris

tbl <- rtable(
  header = rheader(
    rrow(row.name = NULL, rcell("Sepal.Length", colspan = 2), rcell("Petal.Length", colspan=2)),
    rrow(NULL, "mean", "median", "mean", "median")
  ),
  rrow(
    row.name = "All Species",
    mean(iris$Sepal.Length), median(iris$Sepal.Length),
    mean(iris$Petal.Length), median(iris$Petal.Length),
    format = "xx.xx"
  )
)

tbl

# custom format
my_format <- function(x, output) {
   paste(x, collapse = "/")
}
tbl3 <- rtable(
  c("A", "B"),
  format = my_format,
  rrow("row1", c(1,2,3,4), letters[1:10])
)
tbl3
 
}
\seealso{
\code{\link{rrow}}, \code{\link{rcell}}
}
\author{
Adrian Waddell \email{adrian.waddell@roche.com}
}
