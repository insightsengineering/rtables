% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{spf}
\alias{spf}
\title{A spin on sprintf}
\usage{
spf(s, ..., envir = parent.frame(), re = NULL, re_group = NULL)
}
\arguments{
\item{s}{format string to be evaluated}

\item{...}{named arguments to be used in formatting of string}

\item{envir}{the environment in which arguments should be source (defaults to
environment in which function is called)}

\item{re}{regular expression to use for stripping named arguments. Expects a
single capture group, which should contain the name of the argument.}

\item{re_group}{regex capture group, used as gsub replacement to strip name
from regular expression match}
}
\value{
a character vector with regular expression named values replaced
  within string
}
\description{
Allows for named ellipses arguments to be injected into
string without needing numerical index. Inspired by Python's format strings.
}
\examples{
# using explicitly named arguments
spf('\%(x)s + \%(y)s = \%(x + y)s', x = 2, y = 3)
## [1] "2 + 3 = 5"

# using globally defined arguments
x <- 3
y <- 4
spf('\%(x)s + \%(y)s = \%(x + y)s')
## [1] "3 + 4 = 7"

# using a dataframe
head(spf('\%(carb)d + \%(wt).1f = \%(carb + wt).2f', envir = mtcars))
## [1] "4 + 2.6 = 4.78" "4 + 2.9 = 4.78" "1 + 2.3 = 4.78" "1 + 3.2 = 4.78"
## [5] "2 + 3.4 = 4.78" "1 + 3.5 = 4.78"

# evaluating code within a format string 
# (not recommended for complex manipulations)
spf('\%(max(vs) / max(carb)).2f', envir = mtcars)
## [1] "0.12"

}
